# DEV values for k8s-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: foodhub.azurecr.io/fhp
  tag: latest

containers:
  api_domain: api.dev.foodhub.pl
  lp_url: http://register.dev.foodhub.pl
  mailer_url: http://mailer.foodhub.pl
  app_env: prod

ingress:
  hosts:
    - host: panel.dev.foodhub.pl
      paths: ["/"]
